import 'package:flutter/material.dart';

class AudioToAudio extends StatelessWidget {
  const new({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Audio To Audio')),
      body: Placeholder(),
    );
  }
}
